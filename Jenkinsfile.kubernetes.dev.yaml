apiVersion: v1
kind: Pod
spec:
  serviceAccountName: default
  containers:
  - name: docker
    image: "docker:latest"
    imagePullPolicy: Always
    command:
    - cat
    tty: true
    securityContext:
      runAsUser: 10000