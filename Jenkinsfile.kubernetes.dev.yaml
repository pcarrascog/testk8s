apiVersion: v1
kind: Pod
spec:
  serviceAccountName: default
  containers:
  - name: maven
    image: "maven:jdk-8-alpine"
    imagePullPolicy: Always
    command:
    - cat
    tty: true
    securityContext:
      runAsUser: 10000